import { atom } from 'recoil';

export const sttText = atom({
    key: 'sttText',
    default: "",
});

export const amount = atom({
    key: 'amount',
    default: "",
});

export const bankName = atom({
    key: 'bankName',
    default: "",
});